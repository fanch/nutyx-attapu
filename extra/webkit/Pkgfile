# Description: Moteur de rendu Web libre 
# URL: http://webkit.org/
# Maintainer: NuTyX core team
# Packager: thierryn1 at hispeed dot ch
# Depends on: curl, dbus, gconf, gnome-vfs, gperf, gstreamer, gst-plugins-base, gtk, gtk-doc, icu, libpng, orbit2, pango, enchant, libsoup

name=webkit
version=1.1.16
release=1
source=(http://webkitgtk.org/$name-$version.tar.gz)

build() {
cd $name-$version
	./configure --prefix=/usr \
	--localstatedir=/var \
	--with-font-backend=freetype \
	--disable-gtk-doc \
	--enable-jit \
	--sysconfdir=/etc \
	--enable-video
	make
	make DESTDIR=$PKG install
}

